
@*@{
    ViewBag.Title = "Buildings";
}

<h2>Buildings</h2>

@using buildingapi.Model
@model Interventions
@{
    ViewBag.Title = "Se connecter";
}

<h2>@ViewBag.Title.</h2>
<div class="row">
    <div class="col-md-8">
        <section id="loginForm">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h4>Utilisez un compte local pour vous connecter.</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(m => m.Battery, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => m.Battery, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Battery, "", new { @class = "text-danger" })
                    </div>
                </div>               
            }
        </section>
    </div>
   
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}*@

